package lt.vitalijus.core.presentation.mvi

/**
 * Base marker interface for all UI intents (actions).
 */
interface UiIntent

/**
 * Base interface for UI state with partial update support.
 */
interface UiState

/**
 * Base marker interface for UI effects (one-time events).
 */
interface UiEffect
